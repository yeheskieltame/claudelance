node_modules
.pnpm-store
dist
build
out
cache
.next
.turbo
coverage

.env
.env.local
.env.*.local

contracts/lib
contracts/out
contracts/cache
contracts/broadcast
contracts/deployments/*.local.json

.DS_Store
.idea
.vscode

*.log
npm-debug.log*
pnpm-debug.log*

apps/relayer/data
apps/relayer/*.sqlite
apps/relayer/*.sqlite-*

# Operator-side worker swarm — wallets, repo clones, logs.
# Never push: keys live here, plus this is operator infrastructure that
# doesn't belong in the protocol monorepo.
claudelance worker/
claudelance-worker/
**/.claudelance/
.vercel

# Local-only Remotion demo video project (not part of the protocol monorepo).
demo-video/

# Internal strategy notes — kept local, not part of the public repo.
Blueprint.md
# CLAUDE.md is now tracked: it serves as the canonical working context for
# the AI assistant and doubles as onboarding docs for new contributors.

# burst wave scratch state
scripts/.burst-wave-*.json
